{
  "name": "workspace",
  "version": "0.0.0",
  "license": "MIT",
  "scripts": {
    "dev": "npm --workspace @workspace/api-server run dev",
    "build": "npm --workspace @workspace/api-server run build",
    "typecheck": "npm --workspace @workspace/api-server run typecheck"
  },
  "private": true,
  "workspaces": [
    "artifacts/api-server"
  ],
  "devDependencies": {
    "prettier": "^3.9.6",
    "typescript": "~5.9.3"
  }
}
